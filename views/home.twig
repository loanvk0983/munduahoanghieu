{% extends 'layout.twig' %}

{% block title %}Trang chủ - Mụn Dừa Hoàng Hiếu{% endblock %}

{% block head %}
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@12/swiper-bundle.min.css"/>
	<link rel="stylesheet" href="/css/home.css">
{% endblock %}

{% block content %}
	<div class="flex flex-col">
		<!-- 1. Header (Already in layout.twig) -->

        <!-- 2. Hero Section -->
		{% include 'components/hero-section-premium.twig' %}

        <!-- 3. Trust Section -->
        {% include 'components/trust-section.twig' %}
		
        <!-- 4. Problem Section -->
        {% include 'components/problem-section_bak.twig' %}

        <!-- 5. Solution Section -->
        {% include 'components/solution-section.twig' %}

        <!-- 6. Features Section -->
        {% include 'components/features-section.twig' %}

        <!-- 7. How it works Section -->
        {% include 'components/how-it-works-section.twig' %}

        <!-- 8. Use cases Section -->
        {% include 'components/use-cases-section.twig' %}

        <!-- Featured Products (Added) -->
        {% include 'components/featured-products.twig' %}

        <!-- 9. Testimonials Section -->
        {% include 'components/testimonials-section.twig' %}

        <!-- Gallery (Added) -->
        {% include 'components/gallery-section.twig' %}

        <!-- 10. Pricing Section -->
        {% include 'components/pricing-section.twig' %}

        <!-- 11. FAQ Section -->
        {% include 'components/faq-section.twig' %}

        <!-- 12. Final CTA Section -->
		{% include 'components/cta-section.twig' %}

        <!-- 13. Footer (Already in layout.twig) -->
	</div>

	<!-- JavaScript cho trang home -->
	
	<!-- Load Swiper JS FIRST before any initialization scripts -->
	<script src="https://cdn.jsdelivr.net/npm/swiper@12/swiper-bundle.min.js"></script>
	
	<script>
		// Initialize Banner Swiper - wait for DOM and Swiper library
		document.addEventListener('DOMContentLoaded', function() {
			// Check if Swiper is loaded
			if (typeof Swiper === 'undefined') {
				console.error('Swiper library not loaded!');
				return;
			}
			
			const bannerSwiper = new Swiper('.banner-swiper', {
				// Optional parameters
				direction: 'horizontal',
				loop: true,
				autoplay: {
					delay: 6000,
					disableOnInteraction: false,
					pauseOnMouseEnter: true,
				},
				effect: 'slide',
				speed: 600,
				
				// Responsive breakpoints
				slidesPerView: 1,
				spaceBetween: 0,
				centeredSlides: true,
				
				// If we need pagination
				pagination: {
					el: '.swiper-pagination',
					clickable: true,
					dynamicBullets: false,
				},

				// Navigation arrows
				navigation: {
					nextEl: '.swiper-button-next',
					prevEl: '.swiper-button-prev',
				},
				
				// Keyboard control
				keyboard: {
					enabled: true,
				},
				
				// Mouse wheel control
				mousewheel: false,
				
				// Touch ratio
				touchRatio: 1,
				
				// Grab cursor
				grabCursor: true,
				
				// Parallax effect
				parallax: true,
			});
			
			console.log('Swiper initialized successfully:', bannerSwiper);
		});
	</script>

	<!-- AOS animation script -->
	<script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js"></script>
	<link href="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.css" rel="stylesheet" />
	<script>
		document.addEventListener('DOMContentLoaded', function() {
			if (window.AOS) AOS.init({ duration: 700, once: true, offset: 60 });
		});
	</script>
{% endblock %}
